# Heavy deps — runs ONLY in GitHub Actions / Modal Labs
# NEVER imported by apps/api/

# Core data
pandas==3.0.3
numpy==2.4.6
pyarrow>=14.0          # parquet read/write (required by pipeline)
scipy>=1.11.0          # stats (used by scoring)

# Data fetching
yfinance==1.4.1
requests==2.34.2
httpx>=0.27            # used by supplement_user_requested_tickers
finnhub-python>=2.4
ddgs>=9.0.0,<10.0.0          # DuckDuckGo news search (core/news_fetcher.py)
finvizfinance>=1.3.0,<2.0.0  # universe_discovery.py screener
reticker>=0.2.0               # news_sentiment.py ticker extraction

# ML
xgboost==3.2.0
scikit-learn==1.8.0
joblib>=1.3.0
lightgbm>=4.3.0,<5.0.0  # LambdaRank-ranker (core/ml_ranker.py)

# Database
psycopg2-binary==2.9.12
boto3==1.43.1          # R2/S3 uploads

# Utility
python-dotenv>=1.0
mistune>=3.0           # email_template.py
tabulate>=0.9.0        # report formatting
vaderSentiment>=3.3.2  # sentiment fallback
openpyxl>=3.1.0        # Excel export
